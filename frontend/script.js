/* [수정]
  - landmark.html이 제거되었으므로, 'fillAttractionList' 함수 전체를 삭제했습니다.
  - DOMContentLoaded에서 'fillAttractionList()' 호출 코드를 삭제했습니다.
*/

// DOM이 로드된 후 스크립트 실행
document.addEventListener('DOMContentLoaded', () => {

  // --- landmark.html (목록 페이지)용 스크립트 ---
  // [삭제] fillAttractionList();

});

// [삭제] function fillAttractionList(){ ... } (함수 전체가 삭제됨)